{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Dialogs for picking sources and output paths, plus revealing the finished archive. All filesystem work happens in Rust commands, so the webview is granted no filesystem access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "opener:allow-reveal-item-in-dir"
  ]
}
